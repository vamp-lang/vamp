# vamp_syntax

This crate contains Vamp's handwritten lexer, parser, and types to represent AST, tokens, spans, syntax errors, etc.

Right now, Vamp's parser implementation directly generates an AST rather than a CST.